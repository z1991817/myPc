## AGENTS.md
 
## 项目概述
 
**项目名称** artImg Pro - Ai 图片处理工具  
**当前版本**：v 0.0.1 
**项目状态**：生产环境运行，正在开发v0.0.1新功能
 
**业务目标**：为用户提供方便便利的图片处理工具
**核心价值**：简单易用、实时协作、数据安全
 
## 技术栈
**前端**：
- react + heroui + tailwindcss 4 
 
## 开发指令
- npm run dev
- npm run build
 
## 架构和约束
- 所有新增组件都要用到heroui的组件，不要自己定义
- 所有页面的图标都使用lucide-react图标库
- 所有接口都维护到api文件下方
- 所有组件必须使用TypeScript
- 组件文件采用PascalCase命名
- Props接口统一定义在组件顶部
- 所有对话必须使用中文交流
- 功能开发完成后自测
- 功能开发完成后优化代码
- 注释写全
- 调优代码，删除冗余代码
- 我不需要nuxt的后端服务，不要启动nuxt的服务器
- 后端服务地址：http://localhost:3000（配置在.env文件中的NUXT_PUBLIC_API_BASE_URL）
## 当前任务
[tasks]

## 中文编码规则
- 所有包含中文文案的源码文件、JSON 文件、Markdown 文件，统一使用 `UTF-8` 编码保存。
- 修改包含中文的现有文件前，先确认文件编码正常；如果发现乱码、替换字符 `�`、异常问号 `?` 混入中文文案，先修复编码，再继续改功能。
- 不允许对包含大量中文文案的文件做粗暴批量替换、整文件重写、脚本化盲改，除非已经先确认编码安全并且有可回退方案。
- 对中文文件优先使用小范围补丁修改；每次改动后都要检查关键中文文案是否仍然正常显示。
- 如果终端显示疑似乱码，不要立刻继续改代码；先直接检查文件内容本身是否包含 `�`、异常截断文本、乱码字符串。
- 涉及中文文案的修复或重构时，优先从 git 历史、原始文案源、现有稳定文件中恢复，不要凭乱码文本继续加工。
- 任何新改动都不能引入新的乱码；如果一次修改导致中文异常，立即停止继续扩散修改，先回到正常编码状态。

## 中文文件修改流程
1. 修改前先确认目标文件编码正常。
2. 发现乱码时，先恢复编码，再做功能修改。
3. 功能修改尽量使用最小补丁。
4. 修改后检查关键中文文案是否正常。
5. 再运行 lint、构建或页面验证。
