# 已完成功能清单

最后整理时间：2026-03-30

## 1. 项目当前定位

当前项目已经不是纯模板，已具备一个面向 AI 图片生成场景的前端站点雏形，包含落地页、生成工作台、作品画廊、支付页、登录注册、我的创作，以及一套基础的全局状态与接口调用能力。

---

## 2. 已完成的核心功能

### 2.1 首页落地页

已完成一个完整的营销型首页，包含：

- 顶部导航与移动端导航
- Hero 区域与核心卖点展示
- 前后对比展示模块
- 技术能力介绍模块
- 作品画廊预览模块
- 定价展示模块
- FAQ 模块
- 页脚信息区

对应页面：

- `pages/index.tsx`

---

### 2.2 AI 图片生成工作台

已完成主生成页 `createNew`，支持较完整的生成流程：

- 文生图
- 图生图
- 动态拉取模型列表
- 根据模型切换可用比例
- 支持 GPT / Banana 系列模型分流
- 支持上传参考图
- 支持输入图片 URL 作为参考图
- 支持最多 10 张参考图
- 生成中占位骨架与加载状态
- 生成结果预览
- 结果图片下载
- 错误弹窗提示
- 从其他页面带参跳转并回填 prompt / model / size / generationType

已接入的生成相关接口：

- `getModels`
- `generateImage`
- `imageToImage`
- `bananaCreateImage`
- `uploadImage`

对应页面与接口：

- `pages/createNew/index.tsx`
- `api/images.ts`

补充：

- 仓库中还存在一个较早版本的生成页 `pages/create.tsx`
- 当前主流程更完整的是 `pages/createNew/index.tsx`

---

### 2.3 公开作品画廊

画廊页已完成以下能力：

- 瀑布流布局
- 懒加载图片
- 无限滚动分页加载
- 图片详情弹窗
- 展示 prompt、模型、尺寸、生成类型
- 一键复制提示词
- “制作同款”跳转到生成页并自动带参
- “引用图片”跳转到生成页图生图模式并自动带参

对应页面与接口：

- `pages/gallery/index.tsx`
- `api/gallery.ts`

---

### 2.4 我的创作

“我的创作”页已完成用户侧作品管理流程：

- 登录态校验，未登录自动跳转登录页
- 分页加载个人作品
- 滚动加载更多
- 展示生成时间、模型、类型、尺寸、提示词
- 复制提示词
- 下载作品
- 继续编辑
- 删除作品
- 删除确认弹层
- 回到顶部按钮

对应页面与接口：

- `pages/my-creations/index.tsx`
- `api/my-creations.ts`

---

### 2.5 登录 / 注册能力

当前已完成两套登录入口：

#### A. 独立登录页

- 用户名密码登录
- 登录失败提示
- 登录成功后写入 token 和用户信息

对应页面：

- `pages/login/index.tsx`

#### B. 全局登录弹窗

- 登录 / 注册双模式切换
- 注册时支持邮箱、验证码、确认密码
- 前端验证码倒计时
- 注册成功 toast 提示
- 登录成功后关闭弹窗并跳转首页

对应组件与接口：

- `components/LoginModal.tsx`
- `components/TopNavbar.tsx`
- `api/auth.ts`

说明：

- `sendEmailCode` 接口已定义
- 当前登录弹窗里的“发送验证码”主要完成了前端校验、倒计时和提示，未实际调用 `sendEmailCode`

---

### 2.6 支付 / 套餐购买页

支付页已完成前端购买流程展示：

- 三档套餐卡片展示
- 套餐高亮推荐样式
- 套餐权益说明
- 点击购买弹出二维码支付弹窗
- 弹窗中联动显示当前所选套餐价格与积分

对应页面：

- `pages/checkout/index.tsx`

说明：

- 当前二维码为前端占位展示
- 尚未看到真实支付回调、订单状态同步等闭环逻辑

---

### 2.7 用户导航与个人菜单

顶部导航已具备较完整的用户态能力：

- 未登录显示登录按钮
- 已登录显示用户名
- 已登录显示积分 / 点数
- 下拉菜单进入“我的创作”
- 下拉菜单退出登录
- 支持桌面端与移动端导航
- 支持跳转首页定价区

对应组件：

- `components/TopNavbar.tsx`

---

### 2.8 全局积分不足处理

已完成通用的积分不足拦截机制：

- 请求响应中拦截 409 错误
- 自动打开“积分不足”弹窗
- 弹窗可引导跳转首页定价区
- 支持跨页面返回首页后自动滚动到 pricing 区域

对应实现：

- `api/request.ts`
- `store/useInsufficientPointsModal.ts`
- `components/InsufficientPointsModal.tsx`
- `pages/_app.tsx`

---

### 2.9 用户状态持久化

已完成基础用户状态管理：

- 持久化保存 token
- 持久化保存用户信息
- 支持局部 patch 用户信息
- 响应接口后自动更新积分字段
- 401 自动清理登录态

对应实现：

- `store/useUserStore.ts`
- `api/request.ts`

---

### 2.10 全局应用基础设施

已完成基础应用层集成：

- HeroUI Provider
- React Query Provider
- next-themes 主题能力
- 全局 ToastProvider
- 全局积分不足弹窗挂载
- next-i18next 国际化接入

对应实现：

- `pages/_app.tsx`

---

### 2.11 国际化与主题切换基础能力

已具备基础国际化与主题切换组件：

- 中英文语言切换组件
- `public/locales/zh/common.json`
- `public/locales/en/common.json`
- 主题切换组件 `theme-switch`

对应组件：

- `components/LanguageSwitcher.tsx`
- `components/theme-switch.tsx`

说明：

- 国际化基础设施已经接入
- 但当前多数业务页面文案仍是直接写在页面里的中文文本，尚未全面切到 i18n 文案源

---

## 3. 已完成但偏展示型 / 非闭环的能力

这些内容页面或 UI 已完成，但更偏“展示层”：

- 首页定价展示
- Footer 订阅输入框
- 支付二维码弹窗
- About 页面基础占位
- Pricing 页面基础占位

对应页面：

- `pages/about/index.tsx`
- `pages/pricing/index.tsx`

---

## 4. 当前可判断的未完全闭环项

以下内容从代码看已经开始做，但还不能算完全完成：

- 支付真实收款与回调闭环
- 注册验证码真实发送链路
- Pricing 独立详情页内容
- About 独立内容页
- Blog 页面内容体系
- 全站文案国际化落地
- 主题切换在主业务导航中的统一接入
- 生成结果“继续修改”按钮的后续动作闭环

---

## 5. 一句话总结

当前项目已经完成了一个 AI 图片产品的主要前台功能骨架，核心主链路已经具备：

- 首页获客
- 登录注册
- 文生图 / 图生图
- 公开画廊
- 我的创作
- 套餐购买入口
- 登录态与积分态管理

如果作为阶段性汇报，可以定义为：

“AI 图片站点前台核心功能已基本完成，支付闭环、内容页完善和部分后端联调仍待补齐。”
